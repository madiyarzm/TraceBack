node_modules/
out/
webview/dist/
webview/node_modules/
*.vsix
.DS_Store
coverage/
.vitest-cache/

# Local secrets — never commit. Only .env.example is tracked.
.env
.env.local
.env.*.local

# Hide all markdown files, reports, and docs (keep only README.md tracked)
*.md
!README.md
docs/
